/**
 * @repo/ui — shared design tokens and cross-platform primitives.
 *
 * Web uses DOM components (Next.js); mobile uses React Native components (Expo).
 * Only genuinely shareable primitives + tokens live here. Platform-specific
 * views stay in their respective apps.
 */
export { tokens } from "./tokens";
export type { Tokens } from "./tokens";
